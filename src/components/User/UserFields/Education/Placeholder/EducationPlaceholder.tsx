import UserPropertyPlaceholder from "@/components/User/Shared/UserPropertyPlaceholder";
import { mdiSchool } from "@mdi/js";

const EducationBothPlaceholder = () => {
	return <UserPropertyPlaceholder text="No education to show" icon={mdiSchool} />;
};

export default EducationBothPlaceholder;
