import StandardCheckbox from "@/components/Shared/StandardCheckbox";
import styled from "styled-components";

export const StyledStandardCheckbox = styled(StandardCheckbox)`
	margin-bottom: 0.75rem;
	> span {
		font-weight: 600;
	}
`;
