import { mdiWeb } from "@mdi/js";

import UserPropertyPlaceholder from "@/components/User/Shared/UserPropertyPlaceholder";

const WebsitesPlaceholder = () => {
	return <UserPropertyPlaceholder icon={mdiWeb} text="No websites to show" />;
};

export default WebsitesPlaceholder;
