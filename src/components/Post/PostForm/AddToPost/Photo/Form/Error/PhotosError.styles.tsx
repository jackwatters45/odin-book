import styled from "styled-components";

export const StyledPhotosError = styled.span`
	font-size: 1.2rem;
	font-weight: 600;
	text-align: center;
	color: ${({ theme }) => theme.colors.errorColor};
`;
